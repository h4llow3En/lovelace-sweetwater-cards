{
  "name": "lovelace-sweetwater-cards",
  "version": "0.2.1",
  "type": "module",
  "scripts": {
    "build": "rollup -c --environment BUILD:production",
    "dev": "rollup -c --watch",
    "dev:ha": "node --env-file=.env.local ./node_modules/.bin/rollup -c --watch",
    "changelog": "git-cliff -o CHANGELOG.md"
  },
  "devDependencies": {
    "@rollup/plugin-json": "^6.1.0",
    "@rollup/plugin-node-resolve": "^15.0.0",
    "@rollup/plugin-terser": "^1.0.0",
    "@rollup/plugin-typescript": "^11.0.0",
    "rollup": "^4.0.0",
    "tslib": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "lit": "^3.0.0"
  }
}
